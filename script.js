document.documentElement.classList.add("js");
const nav=document.getElementById('nav');let last=0;window.addEventListener('scroll',()=>{const y=window.scrollY;nav.classList.toggle('scrolled',y>40);nav.classList.toggle('hide',y>last&&y>180);last=y});const menuBtn=document.getElementById('menuBtn');const menu=document.getElementById('menu');if(menuBtn&&menu){menuBtn.addEventListener('click',()=>menu.classList.toggle('open'))}document.querySelectorAll('.menu a').forEach(a=>a.addEventListener('click',()=>menu&&menu.classList.remove('open')));const bg=document.querySelector('.hero-bg');document.addEventListener('mousemove',e=>{if(innerWidth<900||!bg)return;const x=(e.clientX/innerWidth-.5)*18;const y=(e.clientY/innerHeight-.5)*12;bg.style.transform=`scale(1.06) translate(${x}px,${y}px)`});const io=new IntersectionObserver(entries=>{entries.forEach(en=>{if(en.isIntersecting){en.target.classList.add('show');io.unobserve(en.target)}})},{threshold:.18});document.querySelectorAll('.reveal').forEach(el=>io.observe(el));const statsSection=document.querySelector('.stats');const counters=document.querySelectorAll('[data-count]');let countersStarted=false;function startCounters(){if(countersStarted)return;countersStarted=true;counters.forEach(el=>{const target=+el.dataset.count;let cur=0;const duration=1500;const start=performance.now();function tick(now){const p=Math.min((now-start)/duration,1);const eased=1-Math.pow(1-p,3);cur=Math.floor(target*eased);el.textContent=(p>=1?target:cur)+'+';if(p<1)requestAnimationFrame(tick)}requestAnimationFrame(tick)})}if(statsSection){const cio=new IntersectionObserver(entries=>{entries.forEach(en=>{if(en.isIntersecting){startCounters();cio.disconnect()}})},{threshold:.7,rootMargin:'0px 0px -18% 0px'});cio.observe(statsSection)}document.querySelectorAll('.faq-box button').forEach(btn=>{btn.addEventListener('click',()=>{const box=btn.closest('.faq-box');box.classList.toggle('open')})});window.addEventListener('error',()=>{document.querySelectorAll('.reveal').forEach(el=>el.classList.add('show'))});setTimeout(()=>{document.querySelectorAll('.reveal').forEach(el=>el.classList.add('show'))},900);
